package interfaces;

public interface FormatInterface {
	// This is the format interface, its applied to all new format objects and ensures that new formats include the methods below
	
	public String getFormattedData();
}
